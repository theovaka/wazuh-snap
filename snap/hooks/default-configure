#!/bin/sh

defaultManager="yah649a5h1ce.cloud.wazuh.com"
defaultPassword="default_password"

manager="$(snapctl get manager)"
if [ -z "$manager" ]; then
       manager="$defaultManager"
fi

password="$(snapctl get password)"
if [ -z "$password" ]; then
       password="$defaultPassword"
fi

mkdir -m 0600 $SNAP_DATA/options
echo "option: $manager" > $SNAP_DATA/options/manager
echo "option: $password" >> $SNAP_DATA/options/password